# HIL Todo — shockarb_lab

> Items requiring human verification or judgment before proceeding.
> Format: `- [ ] TICKER — issue description — suggested fix`

---

## Open Items

### Data Quality: fundamentals.csv coverage gap (2026-08-19)

- [ ] **FUNDAMENTALS-CSV-PARTIAL-COVERAGE** — `data/fundamentals.csv` (as of 2026-08-18 09:23 write) only has 10 of the 66 ukraine_shock universe tickers (ADBE, ASML, DAL, INTU, KLAC, MA, NOW, PLTR, SNPS, UAL). Found while diagnosing the two-day "no recommendations" incident (see `NAN-CLOSE-CACHE-CORRUPTION` below): a live 8/17→8/18 rescoring run produced ETN/LRCX/TXN as WATCH-tier candidates with strong r²/conf.Δ (0.67-0.88 / +0.021-0.033) but blank price/analyst-target, because none of the three are in this file — meaning their real upside could never be checked, and they may have been legitimate Act-on picks. Likely the same partial/failed run (probably `news_scanner.py`'s fundamental_scanner step) that produced the NaN price rows below — worth checking whether it hit the same network issue. — Fix: re-run `python utils\news_scanner.py` (needs live network — can't be done from this sandbox) to refresh `fundamentals.csv` to the full universe, then re-check whether ETN/LRCX/TXN clear the upside gate. — **Update 2026-08-26**: still exactly 10/66 coverage, and now worse in a new way — all 10 current rows (INTU, CRM, PANW, NOW, PLTR, IDXX, ADBE, AMAT, FIS, CRWD — today's top-10-by-confidence_delta) have `Price`, `Fwd P/E`, `TTM EPS`, etc. all `ERR`, not just missing rows for tickers outside the top 10. Ken ran `get_analyst_targets.py --tickers INTU CRM --update-fundamentals` (Finviz provider) and it correctly patched the `Analyst Tgt` column in place (INTU $430.07, CRM $239.86) without disturbing other rows — that tool works as designed. But `stockfit/features.py`'s upside calc requires both `price` and `target` truthy (`if price and target and price > 0`), so with Price still `ERR`→`None`, INTU and CRM still show "analyst target unavailable" in the report despite the target now being known. The Price/Fwd-P/E/EPS fields only come from `fundamental_scanner.py`'s live `yf.Ticker(...).info` call inside `news_scanner.py` — a different pipeline than `get_analyst_targets.py`'s Finviz call — and that yfinance call is failing for literally every ticker currently in the file, not a partial/selective failure. Recommended next step: re-run `python utils/news_scanner.py` (no `--tickers` override needed — INTU/CRM are already #1/#2 by confidence_delta today, so they'll be included in the default `--top 10` pull) from a machine with live network. Caveat worth knowing before running it: `news_scanner.py` fully overwrites `fundamentals.csv` with only the tickers it fetches (`pd.DataFrame(...).to_csv(...)`, no merge) — not destructive today specifically, since coverage is already capped at 10, but worth remembering next time this comes up on a day with broader existing coverage. — **Update 2026-08-26**: added retry with exponential backoff to `fundamental_scanner.py`'s two yfinance network calls (`.info` via new `_fetch_ticker_info()`, `.earnings_dates` inside `_next_earnings()`), both routed through a shared `_call_with_retry()` helper — 3 retries (4 attempts total), delay = 1s/2s/4s + jitter, tunable via `_MAX_RETRIES`/`_RETRY_BACKOFF_BASE`/`_RETRY_JITTER`. Chose 3 over Ken's suggested 5 or 10 because backoff spacing, not raw attempt count, is what actually rides out a rate-limit window (seen to clear within seconds both times this happened); more attempts without adequate spacing just hammers the same limit harder, and a genuine multi-minute outage isn't fixed by retry count either way — only by trying again later, same as happened manually both times this recurred. A ticker with a genuinely empty `.info` (no exception, just sparse data) is deliberately NOT retried — that's a legitimate result, not a failure, and retrying it would risk turning real "thin ticker" cases into false failures. 8 new tests (`tests/test_fundamental_scanner.py`), full suite 871 passing. This does not fix the separate 10/66-coverage cap (still open, same root cause as before — `news_scanner.py`'s own `--top` default and full-overwrite behavior), only the transient-failure-within-a-batch problem.

### Data Quality: Industry Classifications

The industry field in score reports is sourced from an external reference (likely NASDAQ/NYSE listing data or yfinance). Several tickers are systematically misclassified.

- [ ] **PH** — Review source of industry assignment. Was "Office Equipment/Supplies/Services"; corrected to "Industrial Machinery/Components". Verify source and fix upstream if possible.
- [ ] **LHX** — L3Harris is a defense electronics/EW systems company. Currently shows "Industrial Machinery/Components" (same bucket as ETN, ROK). Should likely be "Aerospace" or a defense-specific category. Skipped in auto-fix to avoid collateral corrections to other valid Industrial Machinery entries.
- [ ] **CPRT** — Copart is an online vehicle auction platform, not a traditional auto dealer. "Services-Auto (Vehicle Auctions)" is a workaround; verify the canonical SIC/NASDAQ category.
- [ ] **QCOM** — Tagged "Semiconductors/Wireless Technology" as a compromise. Source data said "Computer peripheral equipment". Confirm correct category vs. peer group (ADI, TXN, KLAC are all "Semiconductors").
- [ ] **HII** — Huntington Ingalls is a shipbuilder; corrected from "Metal Fabrications" to "Aerospace". Confirm whether "Shipbuilding" is a distinct category in the source data.

### Data Quality: live_alpha_us.csv / RVOL cache (2026-06-11)

- [ ] **FTNT row truncated** — `data/live_alpha_us.csv` row 65 (FTNT, last row) has only 8 of 9 columns; the trailing `confidence_delta` value is missing, so `csv.DictReader` reads it as `None`. — `stockfit/features.py` (`_load_scores`/`_load_fundamentals`) now treats missing/`None` fields as empty/NaN so `python -m stockfit report` no longer crashes, but FTNT's `confidence_delta` is NaN and it's excluded from scoring as a result. Re-run `shockarb score` to regenerate `live_alpha_us.csv` cleanly and confirm FTNT gets a real confidence_delta.
- [x] **RVOL shows "—" for all tickers** — RESOLVED 2026-06-11. Root cause was NOT the sandbox-parquet issue noted below — Ken's local cache (`data/prices/daily/{ETN,HON,ISRG}.parquet`, 171 rows each, current through 2026-06-10) was healthy. The real bug: `_compute_rvol()` in `stockfit/features.py` looked for column `("Volume", ticker)`, but the actual cached parquet schema uses LOWERCASE column names (`volume`, `adj_close`, `adj_factor`, `close`, `high`, `low`, `open`) — confirmed via a one-off diagnostic script. `_compute_rvol` now checks for both `"Volume"` and `"volume"`. Added regression test `test_lowercase_volume_column_supported` (tests/test_stockfit_rvol.py). 593 tests pass. Ken should re-run `python -m stockfit report --rvol` to confirm real RVOL values now populate. (The sandbox-mount "Parquet magic bytes not found" symptom seen earlier this session was a separate, sandbox-only artifact and is not related to this fix.)

### Data Quality: fundamentals_cache.json / forward P/E

- [ ] **INTU-FWD-PE** — `fundamentals.csv` shows INTU forward P/E as 9.8x; Schwab/Argus confirms it is ~39.0x current forward. Both CRM and INTU show 9.8x in the same report — suspicious identity suggests a data collision (INTU row may be reading CRM's cached `forwardPE` field). — Inspect `data/fundamentals_cache.json` for the INTU entry; check `fundamental_scanner.py` for any dict-key collision or off-by-one in the yfinance `.info` pull. Also verify that `fundamentals.csv` INTU row is not a stale cache entry predating a ticker change. — **Update 2026-08-26**: INTU and CRM paired up again in a fundamentals failure, though a different symptom this time — both show `Fwd P/E`/`Price`/every yfinance-sourced field as `ERR` rather than a shared wrong number (see `FUNDAMENTALS-CSV-PARTIAL-COVERAGE` above for full detail). Not confirmed as the same root cause as the original P/E-collision finding — could just be coincidence, since literally all 10 tickers in today's file show the identical `ERR` pattern, not just these two — but INTU/CRM recurring together in fundamentals-pipeline problems twice now is worth keeping in mind if a third instance shows up.
  - **Update 2026-07-07**: `stock_report_20260707_1257` shows INTU fwd P/E = 10.0x, CRM = 10.7x. Ken checked a live source and got INTU 11.43x, CRM 11.72x — close, not identical, and not the same failure mode as the original collision (which showed both at an identical 9.8x). Given both names are down ~43-44% from their analyst targets in this scenario, a low-double-digit forward P/E is consistent with a compressed price on flat earnings estimates, not a bug. Retracting the caution on INTU/CRM's upside numbers raised this same day — upside% is driven by price vs. analyst target, not fwd_pe, and that math checks out independently. The original 9.8x/9.8x collision (row above) may still be worth a source-level look someday, but is not implicated by today's numbers.

### LLM Narrative Quality: stock_report (2026-06-23 21:48)

- [ ] **ETN-NAME** — LLM narrative in `reports/stock_report_20260623_2148.md` calls ETN "Eaton Vance Tax-Managed Global Diversified Equity Income Fund". ETN is Eaton Corporation (industrial conglomerate, power management). Same root cause as CPRT/Coinbase — LLM inferring company name from ticker rather than using a lookup. Fix: inject `resolve_name(ticker)` from `shockarb/names.py` into `_build_prompt()` in `utils/stockfit/llm.py`.

### LLM Narrative Quality: stock_report (2026-06-11 20:43)

- [ ] **CPRT** — `--llm` narrative in `reports/stock_report_20260611_2043.md` calls CPRT "Coinbase" ("Coinbase's price of $31.06 reflects an overreaction..."). CPRT is Copart (vehicle salvage auctions), not Coinbase (COIN). The LLM appears to be confusing the ticker with an unrelated company name. — Check `utils/stockfit/llm.py` prompt: is company name passed in, or is the LLM inferring it from the ticker alone? If this report feeds the training corpus (`docs/corpus/`), this bad narrative should be excluded/corrected before use.
- [ ] **MSFT/V cluster tagging** — Both MSFT and V are annotated "Mega-Cap Tech" cluster risk in the same report. Visa is a payments/financial-services name, not tech — grouping it with MSFT under "Mega-Cap Tech" looks like a sector-classification bug in the cluster annotation logic (`stockfit/rules.py`). — Verify sector source and cluster grouping logic; may share root cause with the industry-classification issues above.
- [x] **Large analyst-upside figures (MSFT +43.7%, CPRT +33.4%, V +25.0%)** — CHECKED 2026-06-11. `data/fundamentals.csv` is internally consistent (Fwd P/E recomputes correctly from Price/Fwd EPS for all three: MSFT 20.2x, CPRT 18.4x, V 21.5x). `data/analyst_overrides.csv` has no entries for MSFT/CPRT/V (only KLAC/QCOM/ISRG), so these targets are raw yfinance consensus, not a parsing bug. The implied re-rating is large (target P/E ~25-29x vs current ~18-22x for all three), which is consistent with analyst targets lagging a sharp price selloff rather than bad data — but that lag means the "upside" may shrink once analysts revise targets down, not because price reverts up. — Treat these upside figures as potentially stale/lagging, not as a hard data error. Re-check after next `fundamental_scanner` run / analyst revision cycle.

### Market Data: Holiday Detection

- [ ] **MARKET-HOLIDAY-FLAG** — `market_data.py` fires a "⚠️ Market open — stale data" warning whenever the snapshot date doesn't match the fetch date, but it does not check for US market holidays. On Juneteenth 2026 (Fri Jun 19), the last US trade date was Thu Jun 18; the report generated Sun Jun 21 showed the warning incorrectly, as if data were missing. — Add a holiday calendar check (e.g. `pandas_market_calendars` or a static NYSE holiday list) so that when `last_date` is the last *trading* day (not just the last calendar day), the stale-data warning is suppressed and the report instead notes: "Last US trading session: {date} ({holiday name} — market closed {skipped date})."

### Position Reconciliation: Entry-Price FIFO Match (2026-07-01)

- [ ] **INTU-ENTRY-PRICE** — Cost basis $263.00/share (5 sh, positions.csv 2026-07-01) does not exactly match any `price` field in `stock_report_*_verdicts.csv` from 2026-06-01 through 2026-07-01 (closest: $261.00 on 07-01, $266.40 on 06-29/06-30 — both >$2 off). Entry likely predates the verdicts-CSV tracking window (first verdicts CSV is 2026-06-18) or was filled at an intraday price not captured by any EOD report snapshot. Verify against brokerage trade confirmation for the actual fill date.
- [ ] **NOW-ENTRY-PRICE** — Cost basis $97.88/share (13 sh) does not exactly match any report price in the same window (closest: $99.28 on 07-01, off by $1.40). Same likely cause as INTU — pre-dates verdicts CSV history or intraday fill. Verify against brokerage records.
- [ ] **SNPS-ENTRY-PRICE** — Cost basis $441.99/share (4 sh) does not match any report price 2026-06-01 to 2026-07-01 (closest $446.07, off by $4.08). Same caveat as above.

### Data Quality: Stale/Undocumented analyst_overrides.csv Entries (2026-07-01)

- [ ] **SNPS-NOW-QCOM-OVERRIDE-AUDIT** — Same file also hardcodes SNPS ($550), NOW ($134), and QCOM ($220). None of these are documented/explained in KT.md either. Given the CRM override was proven stale, these three should be spot-checked against live consensus before being trusted as exit-price references — the whole file may be a one-time fix that's been silently masking analyst-target drift ever since.
  - **Update 2026-08-06**: QCOM checked. $220 isn't a clean outlier vs. multi-source consensus *averages* (TipRanks $205.96, Zacks $202.58, MarketScreener $220.57 — checked today), so this isn't the same clear-cut staleness as CRM's $290. But the two most recent individual analyst *actions* (UBS → $170 on ~8/1, TD Cowen → $175) are both well below $220 and below what the report needs to clear a meaningfully-positive upside gate. Worth deciding whether the override should track trailing consensus average (current behavior, lags recent cuts) or most-recent-action (would show ~+8-11% upside instead of +39.7% today). SNPS/NOW not rechecked this pass.
  - **Update 2026-08-11**: QCOM flagged again (5th occurrence: 7/22, 8/3, 8/6, 8/11). New data point: BofA cut its QCOM target to $180 (from $220), maintaining Underperform. Broad consensus average is still $205.96 (TipRanks), below the $220 override. Every specific analyst action checked over the last two weeks now points the same direction — UBS $170 (~8/1), TD Cowen $175 (8/1), BofA $180 (8/11) — putting real upside off the freshest actions at roughly +11-17%, not the report's stated +35.7%. The consensus-average-vs-most-recent-action question from 8/6 is no longer close: three independent recent actions all cluster well below $220. SNPS/NOW still not rechecked.
  - **Update 2026-08-12**: No new stock_report has landed since 8/11 (most recent on disk as of Wednesday 8/12 premarket is still `stock_report_20260811_1011.md`, describing Monday 8/10's close — now missing the entire Tuesday 8/11 session, ~27h stale). Reviewing the same static QCOM pick again: live price has moved to ~$166.41 (from the report's $162.17), and broader analyst consensus has kept drifting down — 48-analyst average now $180.60 (TipRanks, checked today), down 5.29% over the past 3 months. Fresh individual cuts since the 8/11 check: Citi to $175 (from $198), Evercore ISI to $159 (from $179), Bernstein to $165 (from $235) — all still well below the $220 override. One counter-data-point: Freedom Broker upgraded QCOM to Buy on 8/6, but didn't republish a target above the consensus band. Real upside off the current consensus average ($180.60) vs. live price ($166.41) is roughly +8.5%, not the report's stale +35.7% (which is itself computed off a stale $162.17 price too). Sixth occurrence of this exact override-staleness pattern for QCOM (7/22, 8/3, 8/6, 8/11, 8/11 dup, now 8/12) — the $220 figure has not moved once across six checks spanning three weeks while every independent input (price, consensus, individual actions) has moved against it.
- [ ] **LRCX-AMAT-SAME-STALE-TARGET-PATTERN (2026-07-06)** — `stock_report_20260706_1326` still excludes LRCX (r²=0.878, conf.Δ=+0.0760, target $348.65 vs price $351.41) and AMAT (r²=0.789, conf.Δ=+0.0441, target $578.91 vs price $603.04) on "analyst target below current price — data quality issue," the same pattern that was blocking KLAC before its target got refreshed to $305.50 (now clearing all gates, +29.7% upside). LRCX's raw signal (r²=0.878) is actually stronger than KLAC's (0.849). — Same fix likely applies: get a current analyst-target consensus for LRCX/AMAT (Schwab, or the `--llm` cross-check flag on `get_analyst_targets.py`) and enter it the same way KLAC's was resolved; check whether either then clears the +5% upside gate.
- [x] **OVERRIDE-MECHANISM-FIX-VERIFIED (2026-07-06)** — Partial confirmation of `OVERRIDE-MECHANISM-NEVER-RAN` below: `stock_report_20260706_1326_verdicts.csv` shows QCOM's analyst_target as exactly $220.00 (its override value), not the broken raw-yfinance $215.42 noted when the bug was found. KLAC also shows $305.50 (its override value). Both are consistent with the `_DATA_DIR` fix having taken effect in a real run. Not a full re-verification (CRM/SNPS/NOW/ISRG weren't checked this pass — CRM's row in this report has blank price/target/fwd_pe fields, worth a separate look), but the two checked here are good signs.
- [x] **OVERRIDE-MECHANISM-NEVER-RAN (2026-07-03)** — RESOLVED (root cause, not just KLAC). `utils/fundamental_scanner.py`'s `_DATA_DIR` defaulted to `Path(os.environ.get("SHOCK_ARB_DATA_DIR", "./data"))` — a cwd-relative literal, unlike `paths.py`'s `__file__`-anchored absolute paths (and unlike `utils/market_data.py`, which anchors its own `SHOCK_ARB_DATA_DIR` fallback to `_PROJECT_ROOT`, the correct existing pattern). Since `docs/KT.md` instructs running everything from `utils/` (`cd utils && python -m stockfit report`), every normal run resolved `_DEFAULT_OVERRIDES` to `utils/data/analyst_overrides.csv` — a path that doesn't exist (confirmed: a stray `utils/data/` dir exists containing only a wrongly-located `fundamentals_cache.json` and two old market_report.md files, no overrides CSV). `_load_overrides()` silently returns `{}` on a missing file by design, so **no override has ever won in a normal `utils/`-directory run** — confirmed by observation: after setting KLAC's override to $305.50 and re-running `news_scanner.py` from `utils/`, `data/fundamentals.csv` came back with QCOM's raw yfinance target ($215.42) instead of its long-standing $220 override. This likely means the CRM-OVERRIDE-STALE and SNPS-NOW-QCOM-OVERRIDE-AUDIT items above were investigating a mechanism that wasn't actually influencing recent reports at all (whatever numbers appeared were raw yfinance, not the override file) — worth re-checking those two once a fresh run confirms overrides are now live. — Fixed: `fundamental_scanner.py` now imports `DATA` from `paths.py` as the fallback anchor instead of the `"./data"` literal, so `SHOCK_ARB_DATA_DIR` still works as an override but the default is cwd-independent. Not yet re-verified end-to-end with a live `news_scanner.py` → `stockfit report` run (blocked on this session's sandbox not reflecting the edited file consistently when read back via bash — logic checked by direct source review, matches the working `market_data.py` pattern). Ken should re-run `python news_scanner.py` then `python -m stockfit report --timestamp` from `utils/` and confirm KLAC/QCOM/CRM/SNPS/NOW/ISRG all show their override values in the new `fundamentals.csv` and report.

### Test Hygiene: pytest pollutes real data/portfolio_sizer.csv (2026-07-01)

- [ ] **PORTFOLIO-SIZER-TEST-POLLUTION** — `tests/test_portfolio_sizer.py::TestExclude` and `::TestTickers` call `generate_orders(...)` without passing `out=`, so they fall through to the function's real default (`./data/portfolio_sizer.csv`). Running `pytest tests/test_portfolio_sizer.py` from the project root silently overwrites the real ticket file with fixture data (BLK/TXN/SNPS/PH/V at $100 mock prices) — found and cleaned up 2026-07-01 after it briefly overwrote real output. Fix: have those two test classes pass `out=None` or a `tmp_path`-based path explicitly, matching the newer `TestMarkPositions`/`TestExecuteLog` classes added the same day, which already do this correctly.

### Tooling / Workflow Enhancements (Backlog)

- [ ] **NEWS-CACHE** — `[News] Refreshing fundamentals for ... candidates` re-downloads news/fundamentals (Yahoo Finance scan) on every `iran_report`/`shockarb_workflows` run, even when only re-debugging the scoring step. Ken wants an option to reuse `data/news.txt` / `data/fundamentals.csv` from a prior run instead of re-fetching, at least for debugging. — Likely a CLI flag (e.g. `--skip-news` / `--cached-news`) in the news-scan step of `shockarb_workflows.bat` / `stockfit.cli`.

### Environment: pandas major-version bump (2026-06-12)

- [ ] **PANDAS-3.0.1** — The `shockarb` conda env now has pandas 3.0.1 (diagnostic run showed `pandas 3.0.1, numpy 2.4.3, python 3.11.14`), a major-version jump from the 2.3.x baseline used during development/sandbox testing. This caused the `pd.concat` `AttributeError: 'Series' object has no attribute 'columns'` at `pipeline.py:816` (concatenating two named Series with differing `.name` values triggers a pandas 3.0.1 regression in `concat.py:489`). — Worked around at the call site (`.rename(None)`), but run the full test suite under pandas 3.0.1 to check for other concat/reshape call sites with the same pattern (two named Series with differing names passed to `pd.concat`).

### Performance Analysis: Executive Overview

- [ ] **BACKFILL-SCORES** — Need weekly score CSVs (Dec 2024 – May 2026) to reconstruct 6-month signal series. Run the backfill loop below on your machine and drop results into `data/backfill/`. — See command in conversation (2026-05-31).
- [ ] **BACKTEST-OUTPUT** — Need `shockarb backtest --model data\ukraine_shock_us_20260528_143030.json --trailing-window 130 --return-type both --holding-periods 1 2 3 5 --top-n 5 --min-r-squared 0.50 --min-confidence 0.005` output. Paste or save to `data/backfill/backtest_output.txt`. — Required for T+1…T+5 mean returns and hit rate table.

### Reporting: Pre-market report label ambiguity (2026-07-07)

- [ ] **MARKET-REPORT-STALE-LABEL** — `market_report_2026-07-07_0856.md` (generated 08:56 ET, before the 9:30 ET open) is titled "2026-07-07 08:56" but its index levels/% changes (SPY +0.87%, QQQ +1.43%, etc.) are actually Monday 2026-07-06's completed session vs. 2026-07-02's close (2026-07-03 was the observed July 4th holiday, so the last close before 7/6 was 7/2). The EOD report `market_report_2026-07-07_1719.md` then shows Tuesday 7/7's real session (QQQ 722.82→709.43, -1.85%) vs. Monday's close. Confirmed by chaining the two: 722.82 (7/6 close, shown in the "morning" report) → 709.43 (7/7 close) = -1.85%, exactly matching the EOD report's own number. An external reader (Gemini, fed both reports) mistook this for a single day's intraday whipsaw ("morning +1.43%, closed at -1.85%") when it's actually two different trading sessions. Not a math bug — the underlying data is internally consistent — but the pre-market report's title/timestamp doesn't make clear it's describing the *prior* completed session, not "today." Suggested fix: label pre-market reports with the session date they describe (e.g. "Session: 2026-07-06") separately from the generation timestamp. Recurred identically on `market_report_2026-07-10_0543.md` (Gemini again read Thursday 7/9's data as "this morning"). — **Update 2026-07-13**: found a second, worse variant — `market_report_2026-07-11_1343.md` is timestamped Saturday 2026-07-11 13:43 ET and its banner literally reads "⚠️ Market open — showing current price vs. close 2026-07-09," which is factually impossible (markets are closed on Saturdays). The underlying data is fine (it's just Friday 7/10's real close, correctly diffed against Thursday 7/9), but the "market open" banner text is a genuine bug — the open/closed check isn't accounting for weekends (and presumably holidays). Worth noting the fix partially exists: this same report type *does* carry an explicit staleness disclaimer (unlike the plain pre-market "Daily close" reports), so the underlying warning mechanism is there — it just needs a correct trading-calendar check, and should be applied consistently to the plain pre-market report type too. — **Update 2026-07-29**: found a new variant, confirmed via the dual_eod run log. `market_report_2026-07-29_0908.md` is titled "Daily close vs. close 2026-07-24" (footer: "Baseline: 2026-07-24") — a 2-session gap that skips Monday 7/27 and Tuesday 7/28 entirely. But the *stock-scoring* step of the same `dual_eod` run (same run, ~1 minute apart in the log) shows its own Provenance block using `adj_close @ 2026-07-28 / adj_close @ 2026-07-27` — i.e., the correct, current Mon→Tue comparison. So within a single `dual_eod` run, the market_report step and the stock-scoring step computed their "most recent close" baseline differently, and the market_report step is the one that's stale. This was caught by comparing two independently-generated `final_review_*.md` files from the same morning (one from the automated `shockarb-morning-review` Cowork task at 09:11, one done manually at 09:16) — the 09:11 version didn't catch this and stated "no staleness concern this run," which is incorrect for the market_report specifically (the stock-scoring data was fine). Worth checking whether `market_data.py` (which generates market_report) and the `shockarb score` step pull their "latest available date" from different logic paths or different cached data. — **Update 2026-08-04**: recurred again, same 2-session-gap pattern. `market_report_2026-08-04_0744.md` (generated pre-market Tuesday 8/4) is titled "Daily close vs. close 2026-07-30" (footer: "Baseline: 2026-07-30"), skipping both Friday 7/31 and Monday 8/3 entirely — confirmed both were normal trading days (no NYSE holiday either date; real S&P 500 closed at a record 7,600.50 on Monday 8/3 per CNBC/TheStreet). The report's own SPY figure (747.03, +0.72%) doesn't match the real index level implied by Monday's close (~760), confirming the underlying price data is genuinely stale, not just mislabeled. This time the stock-scoring side has zero Act-on/Watch candidates and uniformly tiny confidence_delta values (max +0.0017, versus the 0.020 threshold), so there's no stock-side provenance timestamp available in this report to cross-check against (unlike the 7/29 case, which could compare against the scoring step's own date). Same underlying question as before: whether `market_data.py`'s "latest available date" logic diverges from the scoring step's, though this run doesn't provide the comparison needed to confirm it independently.
- [ ] **CPRT-MISSING-FUNDAMENTAL-CONTEXT** — **Update 2026-07-22**: sixth occurrence, CRM again (third time for this ticker specifically, after 6/23 and 7/15). `stock_report_20260722_1258.md`'s sole "Act on" pick is CRM (empty `news_headlines`, narrative says nothing about any negative catalyst), but Morgan Stanley downgraded CRM on 2026-07-21 — one day before this report — with shares dropping 3.6% in premarket on the news. This is about as fresh and material as a missed catalyst gets: a same-week sell-side downgrade sitting entirely outside the report's view. Separately, and independent of the news-pipeline gap: current live analyst consensus (stockanalysis.com/marketbeat, 52-64 analysts, checked 2026-07-22) puts CRM's target around $245-255, not the $290 this report shows — consistent with the still-open CRM-OVERRIDE-STALE item below, which first flagged this exact override as stale on 2026-07-01 and has evidently still not been corrected three weeks later. Combined effect: the report's headline "+70.5% upside" is inflated by a stale target AND silent on a fresh downgrade — both errors point the same direction (overstating the case for CRM). — Every ShockArb report since 2026-07-07 has carried CPRT with an empty `news_headlines` field and language like "no adverse news directly impacting its essential auto salvage auction business" / "indiscriminate macro panic rather than fundamental deterioration." Real news (checked 2026-07-13) tells a different story: CPRT is down ~25.6% YTD and recently hit a 52-week low; CEO Jeff Liaw is stepping down effective 2026-07-31 with founder Jay Adair returning as CEO; fiscal Q3 2026 net income fell 1.0% YoY despite revenue growth (margin softness). None of this is "macro panic" — it's a real leadership transition plus a slight profitability miss, and it's the same failure pattern seen on CRM (KeyBanc/Bernstein downgrades not surfacing as "adverse news" either). The local news.txt/fundamentals pipeline appears to systematically miss or under-weight slower-moving company-specific stories (management changes, earnings internals) in favor of same-day headline scraping. Worth checking why CPRT's own CEO-transition headline (seen in the 7/7 catalyst feed as "How CEO Transition and Russell Reclassification Will Impact Copart (CPRT) Investors") didn't get attached to CPRT's row in later reports, and whether the upside/reversion narrative should be qualified whenever a ticker has a recent management change or earnings miss on file. — **Update 2026-07-14**: third occurrence, same pattern, different ticker. `stock_report_20260714_1322.md`'s sole pick, LRCX (empty `news_headlines` again), is described as "overlooking the company's fundamental strength" amid "broader semiconductor concerns." Real news for the 7/13 session it's describing tells a much more specific story: LRCX fell 5.8% on (a) cooling AI-driven memory demand, (b) reported yield hurdles in customers' transition to next-gen Gate-All-Around (GAA) architecture — a real, specific technical/execution risk that could delay tool orders, and (c) margin-pressure concerns tied to 3D-DRAM R&D/supply-chain costs. Also worth noting: LRCX is up ~93% YTD, so this is a pullback within a big rally, not a stock in a structural decline like CPRT — a materially different risk profile that the report's uniform "macro panic, fundamentally sound" framing doesn't distinguish. Three-for-three now (CRM, CPRT, LRCX) on the model's top/sole pick having real, checkable news the local pipeline missed — this is a systemic gap, not a one-off, and probably warrants treating every "no adverse news" claim in these reports as unverified until checked against a live source. — **Update 2026-07-15**: found the root cause of at least part of this pattern, and it's worse than "missing" — the relevant headline is sometimes *already in the file*, just attached to the wrong ticker. `stock_report_20260715_1213.md` lists CRM as a top pick ("no recent negative company news," +73.1% upside) with an empty `news_headlines` field. But the same file's NOW row (excluded on earnings-imminent grounds) carries this headline verbatim: "ADBE, CRM, NOW, MSFT: Software Stocks Fall After IBM Warns Clients Shifting Spending To Servers, Cybersecurity" — CRM is named explicitly in a headline sitting in the same report, just filed under NOW instead. Real news confirms this is serious: IBM crashed ~25% on 2026-07-14, its worst day on record (worse than Black Monday 1987), after warning that enterprise clients are shifting Q2 capex from software toward hardware (memory/servers, ahead of expected price increases) — a genuine, structural software-demand headwind, not macro noise. There's also a specific, separate thread: IBM's CEO said the release of Anthropic's "Mythos" model stalled several large software deals over customer security concerns. This is a real, large, well-covered story directly naming CRM, and the report missed it entirely on CRM's own row. Root cause: `news_scanner.py` fetches headlines per-ticker and attaches them only to the ticker whose query triggered the fetch — a multi-ticker headline like this one only lands on whichever ticker's `yf.Ticker(x).news` happened to surface it (NOW, here), never cross-attached to the other named tickers (ADBE, CRM, MSFT) even though their symbols are right there in the title. The 2026-07-14 keyword-severity-tagging fix doesn't help here either — "IBM Warns Clients Shifting Spending" doesn't match any of the `_SEVERITY_KEYWORDS` categories ("warns" isn't in the GUIDANCE list). Two separate gaps now confirmed: (1) real stories the fetch never surfaces for the affected ticker at all, and (2) real stories the fetch *does* surface, attached to the wrong ticker, that keyword tagging can't rescue because the keyword list doesn't cover every phrasing. A fix for (2) — scanning fetched headlines for other tracked tickers' symbols/names and cross-attaching — would be a smaller, more surgical change than full multi-source integration and would have caught this exact case. — **Update 2026-07-20**: fifth occurrence, and arguably the most serious yet — this time a real *moat* threat, not just a demand wobble. `stock_report_20260720_1231.md`'s top pick is SNPS (conf.Δ=+0.0536, the strongest signal seen in any of these reports), empty `news_headlines`, +43.1% upside framed as a clean dislocation. Real news: "Synopsys (NASDAQ:SNPS) Stock Sinks as Kimi Chip Demo Tests the EDA Moat" — Moonshot AI's Kimi K3 model reportedly designed a chip autonomously using open-source EDA tools, directly threatening the core competitive moat of Synopsys's flagship business (EDA software), not a cyclical or macro story. Real consensus target ($567.57, 15 of 20 analysts Strong Buy) is reasonably close to ShockArb's $550 figure, so the target itself isn't stale here — the miss is purely on the news side. Given the unusually high confidence_delta on this name, this is a case where a very strong quantitative signal is being generated by a real, negative, business-model-level catalyst — the opposite of reassuring. Five for five now (CRM ×2, CPRT, LRCX, SNPS) on the top/sole pick carrying a real catalyst the pipeline didn't surface. At this point, "no recent negative company news" in these reports should be read as "not yet checked," not as a finding. — **Update 2026-07-23**: seventh occurrence, CRM again (fourth time for this specific ticker: 6/23, 7/15, 7/22, now 7/23). `stock_report_20260723_1248.md` again lists CRM as an "Act on" pick, narrative saying "despite the absence of recent negative company-specific news" with +77.9% upside off a $290 analyst target. Real news, checked today: Morgan Stanley downgraded CRM Overweight→Equal-Weight on 2026-07-21 (two days before this report), cutting its target from $287 to $185 — shares dropped 3.6% on the news. That's the same week as this report, and MS's own $185 target implies only ~13% upside from the $163 price used, not +77.9%. This stacks on top of the still-unfixed CRM-OVERRIDE-STALE item below: live consensus checked today (52-64 analysts) is ~$245-255, not $290 — real upside is roughly 50-56%, not 78%. Combined effect is identical to 7/22: inflated target + silent on a fresh, material downgrade, both errors overstating the case for CRM. The override has now sat unresolved for over three weeks (first flagged 2026-07-01). — **Update 2026-08-03**: CPRT recurs again as an Act-on pick (`stock_report_20260803_1321.md`), same framing ("market underestimating resilience of essential auction services"), still omitting real fundamental softness: insurance auction volumes down (global -2.7%, US -4.2% per Q3 results), stock -38.5% over 52 weeks vs. S&P 500 +16.8%. Same day, QCOM also flagged: real, ongoing handset/Apple-modem revenue softness (Q3 revenue ~flat/-4% YoY) that the report's "macro panic" framing doesn't surface — management's own language is "diversifying away from handsets," not "handsets are fine." Separately, LRCX was a useful new variant worth recording: not hidden bad news, but a narrative *mismatch* — the report calls it an "overreaction that should revert" from "industry headwinds," but LRCX actually beat Q4 earnings badly and rallied 18% on 2026-07-29 guidance; the signal may still be valid but the story told about it doesn't match what happened. On the positive side, ASML's China-DUV threat (flagged 2026-07-28) has genuinely cooled — stock rebounded ~6.8% as analysts judge the threat less immediate — a rare case of a flagged risk resolving favorably rather than compounding. — **Update 2026-08-05**: CPRT recurs again as an Act-on pick (`stock_report_20260805_0824.md`), still with empty `news_headlines` and the same "market overreaction, not fundamental deterioration" framing (+37.1% upside off a $40.30 target). This is the most severe instance of the pattern yet. Since the last check (8/3): Barclays cut its price target to $26 (from $32) on 2026-07-21 while maintaining an Underweight rating — a target that sits *below* the current $29.40 price, meaning the most bearish sell-side view on record is calling for further downside, not the reversion the report claims. More seriously, multiple law firms (Pomerantz, Kessler Topaz Meltzer & Check, Bragar Eagel & Squire) have opened securities-fraud investigations into Copart and its officers following CEO Jeff Liaw's abrupt resignation effective 2026-07-31, and Liaw sold 27,745 shares (~$846K, average $30.49) on 2026-07-28, three days before his exit took effect. None of this — the Underweight rating, the sub-current-price target, or the fraud investigations — appears anywhere in the report's CPRT narrative or news_headlines field. In fairness to the model: the broader analyst consensus (~$37-48 range across sources, mostly "Hold") is roughly in line with the report's $40.30 target, so the target itself isn't stale the way CRM's was — but a blanket "no adverse news" claim is indefensible when an Underweight rating with a below-market target and active fraud litigation both exist and weren't surfaced. — **Update 2026-07-28**: eighth occurrence, but a more nuanced one worth recording as a distinct variant. `stock_report_20260728_1244.md` lists three Act-on picks — ASML, AMAT, ADI — all framed as "macro-driven panic rather than fundamental deterioration" amid a semiconductor sell-off. Unlike prior occurrences, the *sector-level* framing checks out: this is a real, large, global AI-spending-sustainability selloff (Bloomberg semiconductor gauge -7.5% in Asia, SK Hynix -13%, Samsung -10%, Kospi -9%, driven by fresh doubts about AI capex returns and "circular AI investment" concerns). So "macro panic" isn't wrong here as a sector-wide description. But two of the three picks still carry a real, ticker-specific fact the report omits: (1) ASML's own headlines in the report cite generic "AI Spending Fears Hit Chip Stocks," but the actual proximate cause of ASML's -8.5% drop on 7/27 was reports that China has begun producing its own domestic DUV lithography machines — a genuine long-term competitive-moat question specific to ASML, not interchangeable with the sector story (JPMorgan called the reaction "disproportionate," so this is a live debate, not a slam dunk, but it's a materially different risk than "swept up with everyone else"). (2) ADI has an empty `news_headlines` field in the verdicts CSV despite a real, recent Piper Sandler downgrade (Overweight→Neutral) following its July-quarter report, explicitly citing ADI's ~50% industrial-segment exposure as vulnerable to a broader economic slowdown — a real analyst action missing entirely from the report's view of ADI. AMAT, by contrast, held up under the same check — no ticker-specific negative news found beyond the sector-wide selloff, earnings not until 2026-08-13. Net: the recurring pattern isn't "the macro framing is always wrong" — sometimes it's genuinely a sector event — but the pipeline still can't be trusted to surface real, material, ticker-specific facts (competitive threats, analyst actions) layered on top of a real macro story. Two out of three picks needed a correction today; only one held up clean. — **Update 2026-08-06**: QCOM recurs as the sole Act-on pick (`stock_report_20260806_1257.md`, conf.Δ=+0.0242, r²=0.747, +39.7% upside off the $220 target), `news_headlines` empty again, narrative purely "macro panic ... temporary undervaluation," no company-specific caveat — third time this exact ticker has been flagged here (7/22 industry-classification note, 8/3 handset-softness note, now again). Real news checked today: Qualcomm's Q3 showed a 4% revenue decline and a 20% EPS drop, with continued pressure described as coming from a broader semiconductor selloff and rising competition from Chinese AI chipmakers — this is the same handset/modem softness flagged 8/3, still not surfaced by the pipeline. More material for today: two real analyst price-target cuts landed in the days just before this report — UBS cut QCOM to $170 (from $190, Neutral) around 2026-08-01, and TD Cowen cut to $175 (Buy maintained) — both well below the $220 figure this report uses. That $220 is the same static `analyst_overrides.csv` entry flagged unverified in `SNPS-NOW-QCOM-OVERRIDE-AUDIT` below since 7/22 — still not resolved three weeks on. To be fair to the model: broader multi-source consensus *averages* are more mixed (TipRanks $205.96, Zacks $202.58, MarketScreener $220.57), so $220 isn't a clean outlier the way CRM's $290 was — but every individual analyst *action* found this week points down, not up, and none of that shows up in the report. Real upside off the two freshest actual targets ($170-175) is roughly +8-11%, not +39.7%. — **Update 2026-08-03** (reviewing stale `stock_report_20260731_1322.md`, the most recent report on disk as of Monday 8/3 — no fresher report has landed): ninth occurrence, CRM again (fifth time for this specific ticker). Report still shows CRM's target at exactly $290.00 with +60.5% upside and no adverse-news mention. Live consensus checked today (S&P Global, 53 analysts): average target $241.72 — real upside from $180.71 is ~28-34%, not 60.5%. The Morgan Stanley downgrade (Overweight→Equal-Weight, target cut $287→$185, 2026-07-21) flagged on 7/22 and 7/23 is still not reflected three-plus weeks later. `CRM-OVERRIDE-STALE` below has now been open over a month with no fix. Separately (not a new adverse-news miss, but another news-pipeline gap on the same report): CPRT's CEO transition (Liaw→Adair) went into effect 2026-07-31, the same date this report was generated, and Jane Pocock's promotion to President took effect 2026-08-01 — neither appears in CPRT's `news_headlines` or narrative, continuing the same silent-pipeline pattern on a different, non-negative-but-material event. — **Update 2026-08-10**: tenth occurrence, new ticker (ZTS), and a clean case of the stale-target variant rather than the missing-headline variant — this one actually had headlines attached, they just weren't followed up on. `stock_report_20260808_1406.md` (most recent report on disk as of Monday 8/10 — no report has landed for the 8/10 premarket run itself, so this review is against Friday 8/7's session, now ~2 sessions stale) lists ZTS as its top Act-on pick, +49.2% upside off a $108.38 analyst target, with `news_headlines` correctly showing "ZTS Q2 Earnings Call Flags Deeper Companion Animal Pressure" and a Burry-13F headline — so the pipeline did surface the earnings-miss headline this time. What it missed: the *analyst reaction* to that same Q2 report (published 2026-08-06, one day before the session this report describes). William Blair downgraded ZTS to Market Perform from Outperform same-day, citing worse-than-expected derm declines, continued Trio share loss, and continued domestic-animal-care decline. Morgan Stanley cut its target to $85 (from $99, with some sources showing an intermediate $99→$115 progression, converging around $85 as the latest), and UBS cut to $80 (from $85, then further to $80 in a later note) — real, dated, post-earnings actions. None of the three are reflected in the report's $108.38 target or narrative. Real upside off the freshest individual analyst targets ($80-85) from the $72.66 price is roughly +10-17%, not +49.2% — still clears the 5% gate, but a materially different risk/reward pitch than the report suggests, and the narrative doesn't mention any of the three post-earnings analyst actions despite citing the earnings call itself. ZTS has no entry in `analyst_overrides.csv`, so this is a raw-yfinance-lag case (target not yet refreshed post-earnings), not the override-file staleness seen in CRM/QCOM — same symptom (inflated upside on a stale target), different mechanism. — **Addendum, same day**: the above was against a stale report (Saturday 8/8's, read by an automated review that ran before Monday's `dual_eod` had finished — see `MARKET-REPORT-STALE-LABEL`-style race condition, not a data bug). A second review against the actual fresh 8/10 `dual_eod` output (`stock_report_20260810_1322.md`, `reports/final_review_20260810_0928.md`) confirms the same pick, same pattern, updated numbers: target had moved to $104.19 (+43.4% upside, still inflated but less than the $108.38/+49.2% seen in the stale report — some target refresh happened between generations). This time the specific miss is different: `news_headlines` for ZTS itself came back empty in the fresh report, and the exact relevant headline — "Zoetis cuts annual forecasts as pet healthcare demand slows" — is present in the report's catalyst feed but filed under IDXX's row instead of ZTS's, the same multi-ticker-headline-misattachment bug root-caused 2026-07-15. Independently confirmed the underlying guidance cut is serious, not just an analyst-target lag: FY2026 revenue guidance cut to $9.12-9.32B (from $9.68-9.96B), adjusted EPS cut to $6.15-6.25 (from $6.85-7.00), dermatology -16%, U.S. companion animal -11%, plus a leadership restructuring announced alongside it — CEO called it "a more difficult market than we've seen in recent years." — **Update 2026-08-12**: eleventh occurrence, new ticker (HON), and a cleaner case than most — the missing headline isn't actually missing from the pipeline's own data, just from the LLM's narrative. `stock_report_20260812_1317.md`'s sole Act-on pick is HON (conf.Δ=+0.0380, r²=0.733, +14.3% upside off a $263.11 target), narrative attributing the dislocation to the recent Honeywell Aerospace (HONA) spinoff creating a "temporary valuation discount," plus a positive Quantinuum/Oracle deal. `data/news.txt`'s HON block's *first and most prominent* headline — "Honeywell Slides 5% As Future Growth Guidance At Chicago Investor Event Dissapoints" (24/7 Wall St., 2026-08-11) — describes a real, HON-specific (not HONA-related) negative catalyst: CFO Mike Stepniak's cautious 2027 EPS commentary at Deutsche Bank's Chicago Industrials Conference sent HON down ~5% intraday the same session this report describes. The narrative ignores it entirely in favor of the two positive angles. Unlike most prior occurrences this isn't a news-pipeline attachment failure (`NEWS-SEPARATOR-WIDTH-BUG`-style) — the headline is right there, unambiguously attached to HON, just not used. On the target: less clear-cut than usual — no override in `analyst_overrides.csv` for HON, and live consensus (TipRanks: 13 analysts/3mo, avg $248.42, range $218-$270; stockanalysis.com: $246.67, range $198-$292, pre-spinoff data) puts $263.11 near the high end but not a clear outlier. Caution: initially found what looked like a much more damning $175 Jefferies price-target cut (from $235, 8/9) and had to verify carefully — that cut is for Honeywell Aerospace (HONA), a separate ticker, not HON. HON/HONA headline conflation is a real risk given the fresh spinoff and worth remembering next time this ticker comes up. Also worth flagging: none of the current consensus figures likely reflect the 8/11 Chicago guidance disappointment yet, since it's too fresh for target revisions to show up in searches — worth rechecking HON's target in a few days once analyst reaction (if any) lands. — **Update 2026-08-13**: no new `stock_report` has landed for the 8/13 premarket run (most recent on disk is still `stock_report_20260812_1317.md`, now ~20h stale — reviewed against Tuesday 8/11's session per the automated morning task, one session behind). Re-verified the Chicago guidance miss independently: confirmed via 24/7 Wall St./TradingKey/Yahoo — HON closed down 5.28% on 8/11 after CFO Mike Stepniak's Deutsche Bank Chicago Industrials Summit presentation showed FY2026 guidance (EPS $8.05-8.35, revenue $19.8-20.0B) well below prior Street consensus ($10.78 EPS, $24.1B revenue) post-spinoff, plus ~$100M separation costs and supply-chain/margin pressure. This confirms yesterday's finding — not a new catalyst, but independent re-confirmation the guidance miss is real and the report's HON narrative (spinoff discount + Quantinuum positive) still doesn't mention it. On the target: found a genuinely confusing data point worth flagging for whoever checks this next — a Jefferies $175 (from $235) cut appears in searches attached to HON, but an earlier finding (8/12 entry above) attributed an identical $175/$235 cut to HONA specifically; the search snippets on 8/13 could not cleanly disambiguate which ticker Jefferies' Sheila Kahyaoglu actually cut, raising the same HON/HONA-conflation risk flagged yesterday. Countervailing and more clearly HON-specific: Barclays raised its HON target to $252 (from $229) citing cleaner post-spin results and stronger automation order trends — a real, dated, HON-only positive data point. Net: consensus (~$246-263 range) still isn't a clean outlier vs. the report's $263.11, so the target-staleness angle remains inconclusive, but the Jefferies $175 figure should not be treated as a confirmed HON data point without clearer sourcing. — **Update 2026-08-19**: code fix shipped for two of the recurring sub-patterns logged here — not a full resolution, since the pipeline can still miss a story at the source entirely (news_scanner.py never fetching it), which neither fix touches. (1) The 7/15 misattachment variant (a multi-ticker headline filed under only one of the named tickers, e.g. the NOW/CRM/ADBE/MSFT case, the 8/10 ZTS/IDXX case): new `utils/news_flags.py::cross_attach_headlines()` copies a headline to every other already-tracked ticker whose symbol appears in it as a whole word, wired into both `stockfit/features.py` and `marketfit/cli.py`'s news loaders. (2) The 8/12 HON variant — headline correctly attached with its `[GUIDANCE]` tag intact, but the LLM narrative built its case around unrelated positive headlines instead: both `stockfit/llm.py` and `marketfit/llm.py` system prompts now explicitly require any `[TAG]`-prefixed headline to be addressed; `stockfit/report.py::build_enhanced()` also gained a hard backstop — `flagged_headlines_missing_from_narrative()` checks each INCLUDE-tier ticker's narrative against its tagged headlines by content-word overlap (not just company-name overlap, which produced false "covered" verdicts in early testing) and surfaces a Data Quality Flag if the narrative never engages with what the tagged headline actually says. 25 new tests across `tests/test_news_flags.py` and `tests/test_stockfit.py::TestReportEnhanced`. Neither fix helps when news_scanner.py never surfaces the story for any tracked ticker in the first place (the CRM Morgan Stanley downgrade misses, the CPRT fraud-investigation miss, etc.) — that remains open and is the harder, unbounded part of this pattern. — **Update 2026-08-20**: twelfth occurrence, and the HON/HONA conflation risk flagged 8/12–8/13 has now materialized for real. `stock_report_20260820_1116.md` includes HON in its Act-on tier (conf.Δ=+0.0201, r²=0.733, +18.6% upside off a $262.95 target); two of its five attached news_headlines are unambiguously about Honeywell Aerospace (HONA), the company that spun off from HON on 2026-06-29 and is now a separate Nasdaq-listed ticker: "Honeywell Aerospace Stock Has Fallen Far Enough. Morgan Stanley Says It's Time to Buy." and "Honeywell Aerospace targets supplier bottlenecks to boost output." Neither headline is about HON (the remaining pure-play automation company); both are being read by the LLM narrative as support for the HON pick. This is the cross-attach/misattachment bug (`utils/news_flags.py::cross_attach_headlines()`, shipped 8/19) doing the opposite of its intended job in this specific case — HONA and HON share enough of the "Honeywell" name that a symbol/name-based cross-attach could plausibly pull HONA stories onto HON's row, though this wasn't confirmed against source; could also simply be news_scanner.py's yfinance query for "HON" surfacing HONA-related results pre-attach. Either way, the fix needed here is different from the CRM/ZTS misattachment cases: those needed a headline copied *to* a ticker it was missing from; this needs a headline *filtered out* because it belongs to a distinct, no-longer-related company that merely shares a name and recent corporate history with the ticker being scored. On the target: HON's live analyst consensus, checked today across three sources (MarketScreener $263.85, WallStreetZen average of 25 analysts $251.50, 16-analyst average $261.69), brackets the report's $262.95 comfortably — target is not stale, this is purely a news-attribution problem. Separately, in the same report: FTNT's analyst target ($160.86, +5.2% upside, just clearing the report's own gate) is well above every current consensus figure found today — stockanalysis.com/S&P Global-style aggregators show $110-115 average (44-52 analysts), with the highest individual target at $180 and lowest at $70; a same-week Aug-4 sell-side downgrade to Hold/Sell on valuation grounds is also absent from FTNT's `news_headlines`. At real consensus, FTNT would show roughly -25% to -29% "upside," i.e. it likely should not have cleared the +5% analyst-upside gate at all — a more severe version of the QCOM/ZTS stale-target pattern, on the ticker that just barely passed the threshold rather than one already comfortably through it. — **Update 2026-08-26**: thirteenth occurrence, new ticker (INTU), and one of the fastest stale-target turnarounds seen in this log — the target went bad on the same calendar day the report was generated. `stock_report_20260826_1222.md`'s sole Act-on pick is INTU (conf.Δ=+0.0327, r²=0.707, +24.3% upside off a $444.50 target), `news_headlines` showing only the generic earnings-reaction and pre-market-futures headlines, no analyst-action detail. Intuit reported Q4 FY2026 results after the close on 2026-08-25 (EPS beat, $4.03 vs. $3.29 est.) but guided FY2027 revenue to $23.28-23.51B (9-10% growth, below the ~$23.72B Street estimate and down sharply from FY2026's 14%), with TurboTax specifically losing share to cheaper DIY alternatives — shares fell as much as 9% in after-hours trading. At least four sell-side firms cut targets same-day/next-day, all clustering at or below the current ~$357-361 price: TD Cowen (Buy→Hold, $504→$304), Wells Fargo (cut to $300), Stifel (raised to $300 from $275, still Hold), Truist (Buy→Hold, $410→$350). Every fresh post-earnings target is in the $300-350 range — real upside off these is roughly flat to negative, not +24.3%. INTU has no entry in `analyst_overrides.csv`, so like ZTS this is a raw-yfinance-lag case (stale pre-earnings consensus average), not a frozen override-file entry like CRM/QCOM. Full writeup: `reports/final_review_20260826_1312.md`.

---

## Resolved Items

*(Move entries here once verified and fixed upstream.)*

### Model Design: r² gate excludes near-threshold panic dislocations (2026-08-21, resolved 2026-08-21)

- [x] **R2-GATE-NEAR-MISS** — `run_dual_eod` returned zero Act-on/Watch candidates for the 8/19→8/20 session (`stock_report_20260821_1203.md`), despite the same-day `market_report_2026-08-21_0802.md` explicitly characterizing conditions as favorable ("unusual pattern of defensive sectors underperforming suggests targeted macro panic rather than broad risk aversion... highly conducive for ShockArb," fit verdict GOOD 7/11). Ran `qa_audit` (`--no-llm`, no API key configured in this sandbox) to check for a repeat of the 8/19 NaN-cache-corruption incident: all data-integrity checks passed clean. Root cause of the zero-candidate result: real, not a bug — a threshold effect. Two healthcare/medtech names — **ISRG** (actual -5.84% vs. expected -1.18%, r²=0.521, confidence_delta=+0.0243, analyst upside +52.3%) and **BSX** (actual -5.08% vs. expected -1.23%, r²=0.612, confidence_delta=+0.0235, analyst upside +27.0%) — both cleared the confidence_delta≥0.020 and analyst-upside≥5% gates comfortably and were excluded solely by the r²≥0.65 model-fit gate. Both were large, real, high-volume moves (confirmed against the parquet cache — no NaN/split artifacts) in Health Care, the single worst-performing sector that session (-1.87%, normally defensive) — lining up exactly with the "targeted panic" pattern the market_report itself flagged. WMT, by contrast, had the largest raw dislocation in the universe (-9.15%, real, volume spiked to 83.5M) but correctly failed both gates (r²=0.190) — the news feed attributes this to a company-specific earnings miss, not macro panic, so the low r² was doing its job there. — **Resolution, 2026-08-21**: computed a statistically-grounded lower r² floor rather than guessing. The live factor model fits on n=46 trading days, k=3 factors (confirmed from `data/iran_shock_us_iran_20260618_035521.json`); a bare F-test critical R² for "distinguishable from zero" at n=46/k=3 is only ~0.14-0.23 (10%/5%/1% significance), and even Bonferroni-corrected for scoring 66 tickers simultaneously (family-wise 5-10%) the noise floor is only ~0.30-0.33 — confirming MIN_R2=0.65 was always an economic/signal-quality bar, not a statistical-significance test. Chose **MIN_R2_WATCH=0.45** for a new **LOW_CONFIDENCE** tier: comfortably above the Bonferroni-adjusted noise floor (real relationships, not scan-induced false positives), and it cleanly captures ISRG (0.521) and BSX (0.612) while still excluding genuinely weak-fit names like WMT (0.19). Implemented in `stockfit/rules.py` (`_evaluate_one`/`evaluate_all` now split INCLUDE vs. LOW_CONFIDENCE by r² band, same conf.Δ/upside gates for both; cluster-risk annotated separately per tier), wired through `stockfit/cli.py` (`--min-r2-watch`, default 0.45) and `qa_audit` (`stats_checks.py`'s `tier_count_sanity`/`upside_sanity`/`cluster_concentration`/`pick_count_vs_history` all now account for the new tier; `llm_validator.py`'s `select_sample()` includes LOW_CONFIDENCE in its candidate pool). `stockfit/report.py`'s `build()`/`build_enhanced()` now render a dedicated "🔎 Lower-Confidence Candidates" table between Act-on and Watch, with its own threshold note. Verified end-to-end against live 8/21 data: 2 LOW_CONFIDENCE picks (ISRG, BSX) now surface correctly. 12 new/updated tests across `test_stockfit.py` and `test_qa_audit.py`, full suite 863 passing.

- [x] **NAN-CLOSE-CACHE-CORRUPTION** — RESOLVED 2026-08-19. Root cause of Ken's "second straight day of zero recommendations" (2026-08-18, 2026-08-19). `datamgr/coordinator.py::_commit_ticker()` committed whatever the provider returned unconditionally, including rows with a real-looking open/high/low/volume but a NaN close/adj_close — almost certainly yfinance handing back a partial/in-progress bar, occasionally mis-dated to the prior session (confirmed on MSFT: 2026-08-17 shows open=490.22, volume=28.25M, but close=NaN, written 2026-08-18 09:20 ET — before that morning's market open, so a genuinely completed Monday close should have been available). Once committed, `coverage()` reported that date as fully cached, so gap analysis never re-fetched it, and `pipeline.py`'s `prices.ffill().pct_change()` silently forward-filled the NaN close from the prior day — producing a fake 0.000% return for every affected ticker and dragging confidence_delta toward zero universe-wide on any day the pipeline read through the poisoned row. Confirmed directly: rescoring the real 8/17→8/18 close-to-close move via a clean, network-fetched (cache-bypassing) path found a genuine signal — KLAC clears every INCLUDE gate (r²=0.849, conf.Δ=+0.0284, +48.5% upside) — that the corrupted production cache had been suppressing. Scanning the full `data/prices/daily/` cache found this was far more widespread than one ticker/date: 104 of 177 parquet files had at least one NaN-price row, spanning many dates back to May 2026 (some are likely legitimate market-holiday placeholder rows — 5/25 Memorial Day, 6/19 Juneteenth, 7/03 pre-July-4th — but several, e.g. 6/10, 6/30, 7/13, 7/22, 7/27, don't obviously correspond to holidays and warrant a closer look if this recurs). Worst-hit: QQQ, IWM, GLD, HYG, XLB, XLI, XLU, ^HSI each had 15-17 bad rows and lost coverage back to 2026-07-31. Fixed two ways: (1) `_commit_ticker()` now drops any row with a NaN close/adj_close before merging and writing — a bar with no closing price is never a valid completed trading day, so refusing to commit it keeps `coverage()` honest and lets the next run re-fetch a real gap instead of building on garbage (5 new tests, `tests/test_coordinator_phase1.py::TestCommitTickerNaNRowRejection`). (2) Repaired the existing damage: wrote a one-off script that reads every `data/prices/daily/*.parquet` file, drops NaN-price rows, and rewrites via the real `ParquetStore.write()` path (not a raw file edit) so the manifest's `coverage()` date_range stays consistent with what's actually on disk — 104 files repaired, 72 already clean, 1 (`TTLT.parquet`, a 34-row all-NaN stray file unrelated to any tracked universe) left untouched since there was nothing valid to salvage. 801 tests pass, `MANIFEST.txt` regenerated (bundle `f2657affc258ecbac9d25e51`). Not yet confirmed against a live `run_dual_eod` run — Ken should run it for real next session and confirm (a) it no longer needs to re-fetch dates it already has clean data for, and (b) if a NaN row shows up again, it gets rejected with a logged warning instead of silently committed. `FUNDAMENTALS-CSV-PARTIAL-COVERAGE` above is a related but separate gap found during this investigation, not covered by this fix.

- [x] **MARKET-REPORT-PARTIAL-FETCH-GAP** — RESOLVED 2026-08-19. Root cause found: `DataCoordinator._download_and_commit()` (`datamgr/coordinator.py`) batches tickers purely by identical cache-gap span (same `coverage()` end date), which is an accident of cache state, not a real relationship between the tickers — it's how QQQ, IWM, XLI, XLU, XLB, HYG, GLD, and ^HSI (otherwise unrelated) ended up in one `provider.fetch()` call together on 2026-08-18. A single bad/rate-limited ticker, or any transient failure in that one batched `yf.download()` call, raised or returned empty and the *entire* group was skipped — `except Exception: continue` / `if raw is None or raw.empty: continue` — with no per-ticker fallback, so unrelated good tickers were collateral damage. Separately, `market_data.py`'s `_fetch_daily()` had no fallback at all for a ticker with no fresh data: any ticker missing from the coordinator's result, or with fewer than 2 non-NaN rows in the trailing 10-day window, became a bare `_row_error()` blank — even when the parquet store still held a perfectly good older value. Two-layer fix, both with test coverage: (1) `coordinator.py::_download_and_commit()` now falls back to `_retry_individually()` on batch exception or empty result — one `provider.fetch()` call per ticker — so one bad ticker no longer blanks its batch-mates (`tests/test_coordinator_phase1.py::TestDownloadAndCommitRetry`, 3 new tests). (2) `market_data.py` adds `_last_known_value()` — reads the ticker's full parquet history (ignoring the 10-day window) and returns the last 2 available rows if the fresh fetch came back empty; `_row_ok()` gained a `stale: bool` field so these rows render normally (status="ok", real numbers — not treated as missing by `features.py`/`rules.py`) but carry a visible marker; `report.py`'s `chg()` helper in both `build()` and `build_enhanced()` appends `" (stale {last_date})"` when set (`tests/test_market_data.py`, 9 new tests; `tests/test_marketfit.py::TestReportStaleTickerMarker`, 2 new tests). 796 tests pass, `MANIFEST.txt` regenerated (bundle `20939d7f1e0449c1de45c4a8`). Not yet observed against a live `dual_eod` run with an actual mid-batch provider failure — the fix is unit-tested against the exact failure mode found, but Ken should watch for a recurrence to confirm the blast radius is actually contained in production.

- [x] **CRM-OVERRIDE-STALE** — RESOLVED 2026-08-07, five weeks after first flagged (2026-07-01). `data/analyst_overrides.csv` pinned CRM's analyst target at $290 the whole time, overstating every report's upside claim by roughly 2x versus live consensus throughout that window (confirmed repeatedly: ~$254-261 on 7/1, ~$245-255 on 7/22, $241.72 on 8/7 via manual web check). Fixed using the project's `get_analyst_targets.py` utility: Ken ran `--tickers CRM --provider finviz --llm CRM`, producing `finviz_vs_llm_analyst_data.csv` with finviz's blended consensus ($237.27), an independent Gemini search-grounded cross-check ($257.48, 58 analysts), and 17 individually dated analyst estimates — including confirmation of the Morgan Stanley $185 (7/21) downgrade already tracked here, plus previously-unseen dates for CLSA $165 (7/20), Evercore ISI $250 (7/14), KeyBanc $290 (7/09 — an *earlier*, higher KeyBanc estimate than their later downgrade action, which carried no republished target), and a batch of ~11 more from 5/28-6/16. Used finviz's $237.27 as the new override value — the tool's designated primary/blended provider, not a cherry-picked single-analyst point estimate — corroborated by the independent Gemini figure and the manual web checks all landing in the same $237-258 band, all far below $290. Updated both `data/analyst_overrides.csv` (the durable override, CRM 290 → 237.27) and `data/fundamentals.csv` (the current cached snapshot, same change — cosmetic only, since this file gets regenerated by `news_scanner.py`/`dual_eod` and the override reapplies automatically on every run). Not yet re-verified against a live report — next `stockfit report` run should show CRM's upside drop from the previously-claimed +55.3% to roughly +27% (237.27 vs. current price 186.77), and it's worth rechecking whether CRM still clears the `analyst upside ≥ 5%` INCLUDE gate at the corrected figure (it should — 27% is comfortably above 5% — but this hasn't been observed with fresh output yet). `SNPS-NOW-QCOM-OVERRIDE-AUDIT` below (SNPS/NOW/QCOM) is unaffected by this fix and remains open.

- [x] **LLM-NARRATIVE-SESSION-MISLABELED** — RESOLVED 2026-08-07. Partial resolution of `MARKET-REPORT-STALE-LABEL` below — this is the LLM-narrative-text variant of that long-running pattern, not the underlying data-staleness variant. Ken caught it directly: `market_report_2026-08-07_0746.md` (generated 07:46 ET, premarket, correctly describing Thursday 8/6's close vs Wednesday 8/5's — the underlying data was NOT stale) opened with "US equities closed lower **today**..." — asserting the session had closed on a day that, at 07:46 ET, hadn't opened yet. Root cause: `_build_prompt()` in `utils/marketfit/llm.py` told the LLM the fetch timestamp and the baseline (prior) date, but never the actual date of the session being described, so the model had no way to know "today" was wrong. Fixed with Ken's design (simplified from an initial overcomplicated weekday-name approach he pushed back on — "is this so hard?"): new `utils/trading_calendar.py` module with `prior_trading_day(d, is_trading_day=None)` (polymorphic — defaults to plain Mon-Fri, accepts a real NYSE-holiday-calendar predicate later without touching call sites) and `session_label(session_date, today)`, which returns "today"/"yesterday" for the two expected cases (`prior_trading_day` makes "yesterday" correctly mean "Friday" on a Monday report, for free) or an explicit `STALE (...)` string if the data is more than one session behind — which is the actual data-staleness bug, now caught directly instead of glossed over with a wrong-but-plausible phrase. `market_data.py::fetch_snapshot()` now exposes the real session date (`session_date`, from SPY's last cached-close row, same derivation pattern as the existing `baseline_date`). `_build_prompt()` passes `SESSION_LABEL`/`SESSION_DATE`/`MARKET_STATUS` into the prompt and the system prompt now instructs the model to use `SESSION_LABEL` verbatim and never imply an in-progress session unless `MARKET_STATUS` is `OPEN`. Intraday mode is handled as a distinct case (live-overlaid prices are always "today, in progress" regardless of what the cached `session_date` says, since intraday mode doesn't update that field). Also relocated and fixed `report.py`'s existing `_market_open_at_fetch()` (now public `trading_calendar.market_open_at_fetch()`, reused by both `report.py` and `llm.py` instead of being duplicated) to also check for weekends — it previously only checked time-of-day, so it could show "⚠️ Market open" on a Saturday, which is the exact banner bug flagged in the 2026-07-13 update to `MARKET-REPORT-STALE-LABEL` below. That was a bug-fix side effect of this change, not the main fix. 23 new tests (`tests/test_trading_calendar.py` + additions to `tests/test_marketfit.py`), including the exact Monday-after-Friday case, the multi-session-stale case, and a live reconstruction of today's actual bug scenario confirming it now produces `SESSION_LABEL: yesterday` instead of asserting "today." Full suite (764 tests) passes. `MANIFEST.txt` regenerated (bundle `2be146a9d72aeedc3e0d870e`, 102 files). Not yet confirmed against a live Gemini call with the new prompt fields — that requires Ken's next `dual_eod` run with an INCLUDE-tier pick or a nonzero picks_df; the deterministic pieces (session labeling, prompt construction) are fully unit-tested, but whether the model actually obeys the new SESSION_LABEL instruction hasn't been observed in production yet. — **Update 2026-08-18**: first live confirmation, and it caught a real gap. `market_report_2026-08-18_0924.md`'s narrative says "STALE 2026-08-14" throughout (executive summary, sector, bond, overseas, ShockArb-fit sections) instead of silently asserting "yesterday" — Monday 8/17 was a normal trading day, so a session dated Friday 8/14 on a Tuesday premarket report is a genuine one-session gap, and the LLM used the STALE branch correctly. The underlying gap itself is a new, different bug — see `MARKET-REPORT-PARTIAL-FETCH-GAP` above — but the labeling mechanism this item fixed is doing exactly what it was designed to do.

- [x] **LLM-JSON-PARSE-FRAGILITY** — RESOLVED 2026-08-07. Found while confirming `NEWS-SEPARATOR-WIDTH-BUG` (below) took effect: the 2026-08-07 `dual_eod` run logged `marketfit.llm:generate_narratives — LLM returned 0 sections` for the market report — the first occurrence of this in the log's entire history (checked all 11 prior runs, 7/23–8/6, none had it). Circumstantial but strong: that same run was also the first time `Loaded news for 10 tickers` appeared (every prior run logged `Loaded news for 1 tickers` — the direct symptom of the separator bug), meaning today's market-report prompt carried a real ~20-line catalyst-headline block for the first time, versus ~1-2 lines on every previous successful run. Root cause of the *0 sections* itself couldn't be confirmed byte-for-byte — the raw LLM response text was never logged, only the parsed section count — but `_parse_narratives()` in both `utils/marketfit/llm.py` and `utils/stockfit/llm.py` required the *entire* trimmed response to be valid JSON (only a wrapping \`\`\` fence was stripped); a model adding even one sentence of preamble or trailing commentary around the JSON — more likely with a larger, messier prompt — would make the whole response silently discarded via a bare `json.JSONDecodeError` catch. Fixed defensively rather than root-caused exactly: both parsers now fall back to extracting the first `{...}` span via regex if the full-text parse fails, and both `generate_narratives()` methods now log the raw response (first 500 chars) at WARNING when parsing yields zero usable sections, so a recurrence is diagnosable instead of another correlation-only guess. Verified: fed both parsers text with preamble/trailing-commentary/code-fence variants of a valid JSON payload — all now parse correctly; actual garbage text still correctly returns `{}`; stockfit's nested `picks_analysis: {ticker: narrative}` structure (which must NOT be filtered like flat string values) still passes through intact. Full test suite (741 tests) passes. `MANIFEST.txt` regenerated (bundle `510f4cd90053090101f38446`). Not yet observed to recur — no way to confirm the exact trigger without Ken's local run producing another raw-response log entry.

- [x] **NEWS-SEPARATOR-WIDTH-BUG** — RESOLVED 2026-08-06. Root cause found for why `news_headlines` has so often been empty on real ShockArb picks throughout the `CPRT-MISSING-FUNDAMENTAL-CONTEXT` log below. `news_scanner.py` writes ticker blocks in `data/news.txt` separated by 95-dash lines, but both `utils/stockfit/features.py::_load_news()` and the duplicated copy in `utils/marketfit/cli.py::_load_news()` split on a hardcoded 87-dash string (`"---" * 29`). Since 95 ≠ 87, `str.split()` still matched (a 95-dash run contains an 87-dash substring) but left 8 residual dashes glued to the front of every chunk after the first — which then failed the `file_lines[0].startswith("[")` check, so **every ticker's news except the first one in the file was silently dropped**, in both the stock report and the market report's LLM narrative input. Confirmed directly: on 2026-08-06, `data/news.txt` had a real, on-topic QCOM headline ("This Week In AI Chips – AI Efficiency Boost Through Multiverse And Qualcomm Collaboration"), but `_load_news()` returned news for only `LRCX` (that day's first block) out of 10 tickers present. Git history shows the "87-dash" assumption was wrong from when the function was first written — not a later drift between generator and parser. Fixed by replacing the hardcoded split with `re.split(r"-{20,}", content)` in both files, so it's robust to the generator's actual separator width. Verified: `_load_news()` now returns all 10 tickers including QCOM's 5 headlines; full test suite (741 tests) passes; `MANIFEST.txt` regenerated (bundle `ea7d84b048c642fa5d4627d9`). This is a plausible *partial* explanation for a large fraction of the empty-`news_headlines` occurrences logged under `CPRT-MISSING-FUNDAMENTAL-CONTEXT` — any day the flagged ticker wasn't news.txt's first block would have lost its news regardless of whether the underlying scan found real headlines. Ken needs to re-run stock/market report generation on his machine (this sandbox has no `ANTHROPIC_API_KEY`/`GOOGLE_API_KEY`) to confirm today's QCOM narrative picks up the now-attached headline.

- [x] **KLAC-OVERRIDE-EMPTY-SPLIT** — RESOLVED 2026-07-03. `data/analyst_overrides.csv` KLAC row was pinned at $214.21 (a stale pre-split-adjacent figure), tripping the "analyst target below current price" exclusion (price $235.55). Ran `python get_analyst_targets.py --file data\fundamentals.csv --provider finviz --llm KLAC` to cross-check via Gemini search grounding: got 6 dated analyst estimates, but 2 (UBS $2,180 on 06-09, Barclays $2,250 on 06-11) predate the ~10-for-1 split noted in KT.md/this file and are still in pre-split dollars — they inflated the raw Gemini mean to a nonsensical $942. The other 4 (Wells Fargo $305 on 06-22, B of A $317 on 06-23, Cantor Fitzgerald $325 on 06-29, Susquehanna $275 on 07-01) postdate the split and cluster tightly. Ken chose to use the mean of those 4 post-split estimates ($305.50) rather than split-adjusting the 2 stale ones — updated `data/analyst_overrides.csv` KLAC row from 214.21 to 305.50. Note: KT.md dates the split effective 2026-06-12; this file's earlier note (below, now superseded) dated it 2026-05-28 from parquet price action — that discrepancy is unresolved but doesn't affect this fix since all 4 estimates used here postdate both candidate dates.

- [x] **ETF-STOCK-DATE-LAG** — RESOLVED 2026-06-12. `score_universe()` (Path A, `pipeline.py`) now intersects `etf_prices.index` and `stock_prices.index` to a `common_idx` before computing `etf_returns`/`stock_returns`, eliminating the 1-day misalignment between ETF and stock fetches (root cause of the "0 candidates" / impossible sample-verification math seen in `us_iran` 2026-06-12 reports). Added `logger.debug(...)` reporting the aligned date range when the raw ranges differ, an `assert etf_returns.name == stock_returns.name` after alignment, and a new test `TestScoreUniverse::test_aligns_mismatched_etf_stock_date_ranges` (deliberately mismatched ETF/stock date ranges, verifies provenance reflects the aligned range). The underlying *why* the coordinator returns mismatched ranges (one fetch a day stale vs. the other) is still unexplained, but the pipeline is now robust to it regardless.

---

## Notes

- Source of industry strings: likely `ticker_reference_cache.json` or the NASDAQ/NYSE CSV listings in `data/`. Check `shockarb/names.py` for the lookup chain.
- If the source is NASDAQ/NYSE CSVs, industry strings come from their "Sector" or "Industry" column and may lag or be coarse.
